import { Default, Minimal, Plain, Nav } from "./Link.style";

class Link extends Default {
  static readonly Minimal = Minimal;
  static readonly Plain = Plain;
  static readonly Nav = Nav;
}

export default Link;
